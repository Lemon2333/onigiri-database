```
CREATE TABLE equipment (
    id INT AUTO_INCREMENT PRIMARY KEY,         -- 裝備唯一識別碼
    name VARCHAR(100) NOT NULL,                -- 裝備內部名稱
    display_name_tw VARCHAR(100),              -- 顯示名稱（繁體中文）
    display_name_jp VARCHAR(100),              -- 顯示名稱（日文）
    display_name_en VARCHAR(100),              -- 顯示名稱（英文）
    type TINYINT NOT NULL,                     -- 裝備類型（如刀、弓、力形技形勾等，對應類型表）
    min_atk INT,                               -- 最小攻擊力
    max_atk INT,                               -- 最大攻擊力
    required_level TINYINT NOT NULL,           -- 裝備需求等級
    durability INT,                            -- 耐久度
    lore LONGTEXT,                             -- 裝備描述或背景故事
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,  -- 資料建立時間
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP -- 資料更新時間
);
```
```
CREATE TABLE equipment_types (
    id TINYINT AUTO_INCREMENT PRIMARY KEY,     -- 類型唯一識別碼
    type_name VARCHAR(50) NOT NULL            -- 類型名稱（如刀、槍、弓等）
);
```
```
CREATE TABLE effects (
    id INT AUTO_INCREMENT PRIMARY KEY,            -- 特效唯一識別碼
    equipment_id INT NOT NULL,                    -- 關聯的裝備 ID
    effect_type VARCHAR(100) NOT NULL,            -- 特效類型（如提升攻擊、增加防禦等）
    effect_condition VARCHAR(255),               -- 特效條件（如 "HP<50%"）
    effect_value_1 DECIMAL(10, 2),               -- 特效數值 1（如提升的具體值或百分比）
    effect_value_2 DECIMAL(10, 2),               -- 特效數值 2（可選，用於多參數特效）
    effect_target VARCHAR(100),                  -- 特效目標（如 "火屬性"、"所有屬性"）
    FOREIGN KEY (equipment_id) REFERENCES equipment(id) ON DELETE CASCADE
);
```
